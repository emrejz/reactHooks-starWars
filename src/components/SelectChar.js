import React from "react";
export class SelectChar extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <select className="selectChar">
        <option>emre afasf as</option>
      </select>
    );
  }
}
